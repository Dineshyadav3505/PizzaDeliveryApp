/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['as2.ftcdn.net', 'chacha.com', 'images.unsplash.com', 'plus.unsplash.com', 'unsplash.com'], // Combine all domains into one array
    },
};

export default nextConfig;